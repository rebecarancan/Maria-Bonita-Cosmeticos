<br><br><br>

<p class="basic-title"><%= "#{t('messages.editing', model: @master_bank.model_name.human)} - #{l(@master_bank.date, format: :short)}" %></p>

<%= render 'form', master_bank: @master_bank %>

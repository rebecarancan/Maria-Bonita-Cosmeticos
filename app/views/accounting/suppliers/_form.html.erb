<%= render partial: 'layouts/navigation' %>

<%= form_with(model: [:accounting, supplier], local: true, html: { class: "basic-grey"}) do |form| %>
  <% if supplier.errors.any? %>
      <% supplier.errors.full_messages.each do |message| %>
        <script>
          $.notify( {message: '<%= message %>'}, { type: 'danger', offset: 80});
        </script>
      <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    <div class="form-row">
      <div class="col-md-6 mb-5">
        <%= form.label :name, class:"label" %>
        <%= form.text_field :name, class:"form-control" %>
      </div>
    </div>

    <div class="form-row">
      <div class="col-md-1 mb-10">
        <%= form.submit t('buttons.save'), class:"btn btn-primary" %>
      </div>
      <div class="col-md-1 mb-10">
        <%= link_to t('buttons.back'), accounting_suppliers_path, class:"btn btn-secondary"%>
      </div>
    </div>
  </form>
<% end %>

<br>


